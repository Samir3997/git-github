package com.maybank.ilm.core.swiftmsghandlers;

import org.springframework.stereotype.Component;

@Component
public class MXMsgHandlerConcreteImpl extends MXMsgHandlerAbstractImpl {
    
    // No additional methods are required unless you want to override any existing methods
    // For example, if there are specific logging or behavior required

    @Override
    public void throwCoverMgsException() {
        // If specific behavior is needed for this method in the concrete implementation,
        // it can be overridden here. Otherwise, the implementation in the abstract class will be used.
        super.throwCoverMgsException();
    }
    
    // You can add more specific methods or override existing ones if needed
    
}


eq(Collections.singletonList(new AccountBalanceManagement()))




MTSequence sequence1 = new MTSequence();
    sequence1.setSeqName("seqNameValue");
    sequence1.setQualifier("qualifierValue");
    sequence1.setFieldName("fieldNameValue");

    MTSequence sequence2 = new MTSequence();
    sequence2.setSeqName("anotherSeqName");
    sequence2.setQualifier("anotherQualifier");
    sequence2.setFieldName("anotherFieldName");

    List<MTSequence> sequences = Arrays.asList(sequence1, sequence2);
