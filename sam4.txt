  doAnswer(invocation -> {
            // Capture the parameters passed to readValue
            String jsonResponse = invocation.getArgument(0);
            TypeReference<?> typeReference = invocation.getArgument(1);

            // Ensure the correct response is mocked
            if (jsonResponse.equals(mockJsonResponse) && typeReference.getType().equals(BnyApiResponseDTO.class)) {
                return mockBnyApiResponseDTO;
            }
            return null;
        }).when(objectMapper).readValue(anyString(), any(TypeReference.class));
